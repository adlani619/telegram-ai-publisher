name: 🤖 Auto-Post Bot (Telegram Only)

on:
  schedule:
    # كل نصف ساعة (48 مرة يومياً)
    - cron: '*/30 * * * *'
    
  workflow_dispatch:
  
  push:
    branches: [ main ]
    paths:
      - 'bot_advanced.py'
      - '.github/workflows/*.yml'

jobs:
  post-to-telegram:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
      
      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests telethon cryptg
      
      - name: 🔑 Restore Telegram session
        run: |
          echo "$USER_SESSION_BASE64" | base64 --decode > user_session.session
          echo "✅ Session restored"
          ls -lh user_session.session
        env:
          USER_SESSION_BASE64: ${{ secrets.USER_SESSION_BASE64 }}
      
      - name: 🤖 Run Telegram Auto-Post Bot
        env:
          # Telegram Configuration
          USER_SESSION_BASE64: ${{ secrets.USER_SESSION_BASE64 }}
          TELEGRAM_CHANNEL: "NextGenTechOfficial"
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          SOURCE_CHANNELS: ${{ secrets.SOURCE_CHANNELS }}
          
          # OpenAI Configuration
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          
          # Settings
          POSTS_LIMIT: "15"
          MIN_CONTENT_LENGTH: "100"
          
          # Facebook (معطل - يمكن تفعيله لاحقاً)
          # FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          # FB_ACCESS_TOKEN: ${{ secrets.FB_ACCESS_TOKEN }}
          # FB_PUBLISH_AS_DRAFT: "false"
          # POST_TO_FACEBOOK: "true"
        run: |
          echo "🚀 بدء البوت في: $(date)"
          echo "📢 القناة المستهدفة: NextGenTechOfficial"
          echo "🕐 التشغيل التلقائي: كل 30 دقيقة"
          python bot_advanced.py
          echo "✅ انتهى البوت في: $(date)"
      
      - name: 📊 Upload logs (عند الفشل)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: bot-logs-${{ github.run_number }}-${{ github.run_attempt }}
          path: bot.log
          if-no-files-found: ignore
          retention-days: 7
      
      - name: 📈 Summary
        if: always()
        run: |
          echo "### 📊 ملخص التنفيذ" >> $GITHUB_STEP_SUMMARY
          echo "- **الوقت:** $(date)" >> $GITHUB_STEP_SUMMARY
          echo "- **رقم التشغيل:** #${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "- **المنصة:** تيليغرام فقط" >> $GITHUB_STEP_SUMMARY
          echo "- **التكرار:** كل 30 دقيقة" >> $GITHUB_STEP_SUMMARY
          if [ -f bot.log ]; then
            echo "- **حجم السجل:** $(wc -l < bot.log) سطر" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "#### 📝 آخر 10 أسطر من السجل:" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            tail -n 10 bot.log >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
