# 🤖 Telegram Content Aggregator Bot

بوت ذكي يجلب المحتوى تلقائياً من قنوات تيليغرام، يعيد صياغته بالذكاء الاصطناعي، وينشره في قناتك.

## ✨ المميزات

- ✅ جلب تلقائي من قنوات تيليغرام متعددة
- ✅ إعادة صياغة احترافية بـ ChatGPT
- ✅ ترجمة تلقائية (إنجليزي → عربي)
- ✅ نشر تلقائي على قناتك
- ✅ جدولة عبر GitHub Actions
- ✅ سجلات تفصيلية لمتابعة العمليات

## 🚀 الإعداد السريع

### 1. الحصول على API Credentials

#### أ) Telegram Bot Token
1. افتح [@BotFather](https://t.me/BotFather)
2. أرسل `/newbot`
3. اتبع التعليمات
4. انسخ التوكن: `123456789:ABCdef...`
5. أضف البوت كمسؤول في قناتك

#### ب) Telegram API ID & Hash
1. اذهب: https://my.telegram.org
2. سجل دخول برقم هاتفك
3. اضغط "API development tools"
4. املأ النموذج:
   - App title: `Content Bot`
   - Short name: `contentbot`
   - Platform: `Desktop`
5. انسخ:
   - `api_id`: `12345678`
   - `api_hash`: `a1b2c3d4...`

#### ج) OpenAI API Key
1. اذهب: https://platform.openai.com/api-keys
2. سجل دخول أو أنشئ حساب
3. اضغط "Create new secret key"
4. انسخ المفتاح: `sk-proj-...`

### 2. إعداد GitHub Repository

#### أ) إنشاء Repository
```bash
# على جهازك
mkdir telegram-bot
cd telegram-bot
git init

# أو على GitHub مباشرة
# اضغط "New Repository"
```

#### ب) إضافة الملفات
1. انسخ جميع الملفات من الأكواد أعلاه
2. رتبها حسب الهيكل التالي:

```
telegram-bot/
├── .github/
│   └── workflows/
│       └── bot.yml
├── bot_advanced.py
├── requirements.txt
├── .gitignore
└── README.md
```

#### ج) إضافة Secrets
اذهب: **Settings** → **Secrets and variables** → **Actions**

أضف **6 Secrets**:

| Name | Value | Example |
|------|-------|---------|
| `TELEGRAM_TOKEN` | توكن البوت | `123456789:ABCdef...` |
| `TELEGRAM_CHANNEL` | اسم القناة | `@your_channel` |
| `OPENAI_API_KEY` | مفتاح OpenAI | `sk-proj-...` |
| `TELEGRAM_API_ID` | API ID | `12345678` |
| `TELEGRAM_API_HASH` | API Hash | `a1b2c3d4e5f6...` |
| `SOURCE_CHANNELS` | القنوات المصدر | `TechNewsAR,AINews` |

**⚠️ مهم**: 
- أسماء القنوات **بدون @**
- افصل بينها بفاصلة `,` بدون مسافات

### 3. اختيار القنوات المصدر

#### قنوات عربية مقترحة:
```
TechNewsAR
AINewsArabic
AndroidWorldAR
AppleNewsAR
CodingTipsAR
WebDevAR
```

#### قنوات إنجليزية:
```
TechCrunch
TheVerge
Engadget
hackernews
arstechnica
```

ضعها في Secret هكذا:
```
TechNewsAR,AINewsArabic,AndroidWorldAR
```

## ▶️ التشغيل

### تشغيل يدوي:
1. اذهب لـ **Actions**
2. اختر **🤖 Telegram Auto-Post Bot**
3. اضغط **Run workflow**
4. اضغط الزر الأخضر **Run workflow**

### تشغيل تلقائي:
- البوت يعمل **تلقائياً كل ساعتين**
- لتغيير الجدول، عدّل `cron` في `bot.yml`

## ⚙️ التخصيص

### تغيير جدول التشغيل

في `.github/workflows/bot.yml`:

```yaml
schedule:
  - cron: '0 */2 * * *'  # كل ساعتين
  
  # أمثلة أخرى:
  # - cron: '0 9 * * *'      # يومياً 9 صباحاً
  # - cron: '0 */6 * * *'    # كل 6 ساعات
  # - cron: '0 8,14,20 * * *' # 3 مرات يومياً
```

### تخصيص أسلوب الكتابة

في `bot_advanced.py`، عدّل الـ `prompt` في دالة `ai_rewrite_content()`:

```python
prompt = f"""
ضع تعليماتك المخصصة هنا...
مثلاً:
- اكتب بأسلوب شبابي
- أضف هاشتاجات
- استخدم إيموجي كثيرة
"""
```

### زيادة عدد المنشورات

في `bot.yml`:

```yaml
env:
  POSTS_LIMIT: "20"  # بدلاً من 10
```

## 📊 مراقبة السجلات

- **في GitHub**: `Actions` → اختر التشغيلة → `post-to-telegram`
- **عند الفشل**: يتم رفع الـ logs كـ Artifact تلقائياً

## 🛠️ استكشاف الأخطاء

### خطأ: "Missing: TELEGRAM_API_ID"
**الحل**: تأكد من إضافة جميع الـ 6 Secrets في GitHub

### خطأ: "Could not find the input entity"
**الحل**: 
- تأكد من اسم القناة صحيح (بدون @)
- القناة يجب أن تكون عامة

### خطأ: "Invalid API ID or Hash"
**الحل**: راجع my.telegram.org وانسخ القيم بدقة

### خطأ: "No content fetched"
**الحل**: 
- جرب قنوات أكثر نشاطاً
- زد `POSTS_LIMIT`

## 💰 التكلفة

- **GitHub Actions**: مجاني (2000 دقيقة/شهر)
- **OpenAI gpt-4o-mini**: ~$0.15 لكل 1000 منشور
- **التشغيل كل ساعتين**: ~$2-3 شهرياً

## 📝 ملاحظات

- البوت يستخدم **حسابك الشخصي** (عبر Telethon) للقراءة
- البوت Token يُستخدم فقط للنشر
- القنوات العامة لا تحتاج اشتراك
- للقنوات الخاصة، يجب أن تكون مشتركاً فيها

## 🤝 المساهمة

Pull Requests مرحب بها! 

## 📜 الترخيص

MIT License - استخدمه كما تشاء!

---

صُنع بـ ❤️ باستخدام Python، Telethon، OpenAI، وGitHub Actions
