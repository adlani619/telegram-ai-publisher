🤖 Telegram Auto-Post Bot
بوت تلقائي لنشر المحتوى على قنوات تيليغرام باستخدام الذكاء الاصطناعي.
✨ المميزات

✅ معالجة النصوص بالذكاء الاصطناعي (OpenAI GPT-4)
✅ ترجمة وتلخيص تلقائي للمحتوى
✅ نشر تلقائي على تيليغرام
✅ معالجة الأخطاء وإعادة المحاولة
✅ سجلات تفصيلية (Logging)
✅ تشغيل تلقائي عبر GitHub Actions

🚀 الإعداد السريع
1️⃣ إنشاء Repository على GitHub
bash# إنشاء مجلد جديد
mkdir telegram-bot
cd telegram-bot

# إنشاء الملفات
git init
2️⃣ هيكل المشروع
telegram-bot/
├── .github/
│   └── workflows/
│       └── bot.yml          # GitHub Actions workflow
├── bot.py                   # السكريبت الرئيسي
├── requirements.txt         # المكتبات المطلوبة
├── .gitignore              # ملفات التجاهل
└── README.md               # التوثيق
3️⃣ إضافة Secrets في GitHub
اذهب إلى: Settings → Secrets and variables → Actions → New repository secret
أضف المفاتيح التالية:
NameValueExampleTELEGRAM_TOKENتوكن البوت من @BotFather123456789:ABCdefGHIjklMNOpqrsTELEGRAM_CHANNELاسم القناة أو معرّفها@your_channel أو -1001234567890OPENAI_API_KEYمفتاح OpenAI APIsk-proj-...
4️⃣ الحصول على Telegram Bot Token

افتح @BotFather على تيليغرام
أرسل /newbot
اتبع التعليمات واحصل على التوكن
أضف البوت كمسؤول (Admin) في قناتك

5️⃣ الحصول على OpenAI API Key

اذهب إلى platform.openai.com
سجل دخول أو أنشئ حساب
اذهب إلى API Keys وأنشئ مفتاح جديد

⚙️ التخصيص
تغيير جدول التشغيل (Cron)
في ملف .github/workflows/bot.yml:
yamlschedule:
  - cron: '0 */2 * * *'  # كل ساعتين
  # أمثلة أخرى:
  # - cron: '0 9 * * *'      # يومياً الساعة 9 صباحاً
  # - cron: '0 */6 * * *'    # كل 6 ساعات
  # - cron: '0 8,14,20 * * *' # 3 مرات يومياً
تخصيص معالجة AI
في ملف bot.py، عدّل دالة ai_translate_and_summarize():
pythonprompt = f"""
ضع التعليمات الخاصة بك هنا...
مثال:
- لخص في 3 نقاط
- استخدم لغة رسمية
- أضف هاشتاجات
"""
إضافة مصادر محتوى
في دالة fetch_content()، يمكنك إضافة:
من RSS:
pythonimport feedparser

def fetch_content():
    feed = feedparser.parse('https://example.com/rss')
    return feed.entries[0].summary
من API:
pythondef fetch_content():
    response = requests.get('https://api.example.com/news')
    data = response.json()
    return data['articles'][0]['content']
🔧 التشغيل اليدوي
محلياً (Local):
bash# تصدير المتغيرات
export TELEGRAM_TOKEN="your_token"
export TELEGRAM_CHANNEL="@your_channel"
export OPENAI_API_KEY="your_key"

# تشغيل البوت
python3 bot.py
عبر GitHub Actions:

اذهب إلى Actions في repository
اختر Telegram Auto-Post Bot
اضغط Run workflow

📊 مراقبة السجلات
السجلات تُحفظ في:

الـ Console في GitHub Actions
ملف bot.log (عند التشغيل المحلي)
يتم رفع السجلات كـ Artifacts عند الفشل

🛠️ استكشاف الأخطاء
خطأ: "Missing required environment variables"

تأكد من إضافة جميع الـ Secrets في GitHub

خطأ: "Telegram API error: Unauthorized"

تحقق من صحة TELEGRAM_TOKEN
تأكد أن البوت مضاف كمسؤول في القناة

خطأ: "OpenAI API error"

تحقق من صحة OPENAI_API_KEY
تأكد من وجود رصيد كافٍ في حسابك

خطأ: "Chat not found"

تأكد أن اسم القناة صحيح (@channel_name)
أو استخدم الـ Chat ID (-1001234567890)

📝 ملاحظات مهمة

⚠️ GitHub Actions لديها حد 2000 دقيقة/شهر للحسابات المجانية
⚠️ تكلفة OpenAI API تعتمد على الاستخدام
✅ يمكنك إيقاف الجدول الزمني وتشغيل البوت يدوياً فقط
✅ البوت يعيد المحاولة تلقائياً عند فشل الإرسال

📜 الترخيص
MIT License - استخدمه كما تشاء!
🤝 المساهمة
Pull Requests مرحب بها!

صُنع بـ ❤️ باستخدام Python و GitHub Actions
